import { Field, Group } from 'o1js/dist/node/lib/core';
import { PrivateKey, PublicKey } from 'o1js/dist/node/lib/signature';
export { encrypt, decrypt };
type CipherText = {
    publicKey: Group;
    cipherText: Field[];
};
/**
 * Public Key Encryption, using a given array of {@link Field} elements and encrypts it using a {@link PublicKey}.
 */
declare function encrypt(message: Field[], otherPublicKey: PublicKey): {
    publicKey: import("o1js/dist/node/lib/group").Group;
    cipherText: import("o1js/dist/node/lib/field").Field[];
};
/**
 * Decrypts a {@link CipherText} using a {@link PrivateKey}.^
 */
declare function decrypt({ publicKey, cipherText }: CipherText, privateKey: PrivateKey): import("o1js/dist/node/lib/field").Field[];
